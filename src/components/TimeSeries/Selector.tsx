import { FC } from "react";

type TimeSeriesSelectorType = {
  duration?: number;
  setDuration?: (duration: number) => void;
};
const TimeSeriesSelector: FC<TimeSeriesSelectorType> = () => {
  return null;
};

export default TimeSeriesSelector;
